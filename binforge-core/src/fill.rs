//! K1: complete an empty grid to a full valid solution. Randomness lives
//! here, never in the solver: binsolve stays bit-deterministic and takes
//! a caller-supplied choice oracle (AR2, binsolve mini-round B1) (L3).
