[package]
name = "binsolve"
description = "CLI for the binsolve binary-puzzle solver"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
binsolve-core = { path = "../binsolve-core" }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
