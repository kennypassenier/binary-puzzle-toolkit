[package]
name = "bpt-forge"
description = "Binary puzzle generator: geometry, fill, carve, grading"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rand = "0.9"
rand_chacha = "0.9"
bpt-core = { path = "../bpt-core" }
serde = { version = "1", features = ["derive"] }
toml = "0.9"

[dev-dependencies]
insta = "1"
