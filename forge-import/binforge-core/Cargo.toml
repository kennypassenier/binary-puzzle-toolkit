[package]
name = "binforge-core"
description = "Binary puzzle generator core: geometry model, solution filling, carve loop, difficulty grading"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.9"

[dev-dependencies]
insta = "1"
