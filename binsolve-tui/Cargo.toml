[package]
name = "binsolve-tui"
description = "TUI for the binsolve binary-puzzle solver"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
binsolve-core = { path = "../binsolve-core" }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
crossterm = "0.28"
ratatui = "0.29"
