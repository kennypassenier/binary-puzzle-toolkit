//! binsolve TUI — replays solver event logs visually (AR9). Real
//! implementation lands in L9; until then this is the L0 skeleton.

#![forbid(unsafe_code)]

fn main() {
    eprintln!("binsolve-tui: not yet implemented — milestone L9 delivers the TUI");
    std::process::exit(2);
}
